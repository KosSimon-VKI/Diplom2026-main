<UserControl x:Class="GardenNookWpf.Views.Shell.Sections.TechnicalCards.TechnicalCardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid x:Name="RootGrid"
          Background="#fff">
        <Viewbox Stretch="Fill"
                 StretchDirection="Both">
            <Grid Width="1200"
                  Height="900">
                <Grid Margin="18,20,18,20">
                    <Border Style="{StaticResource ModalCardBorder}"
                            Background="{StaticResource ModalColorWhiteBrush}"
                            Padding="14">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0"
                                  Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="230"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="SearchTextBox"
                                         Height="46"
                                         FontSize="18"
                                         Padding="12,8"
                                         BorderBrush="{StaticResource ModalColorBorderBrush}"
                                         BorderThickness="2"
                                         VerticalContentAlignment="Center"
                                         ToolTip="Поиск по названию"
                                         TextChanged="SearchTextBox_TextChanged"/>
                                <Button x:Name="AddTechnicalCardButton"
                                        Grid.Column="2"
                                        Content="Добавить техкарту"
                                        Style="{StaticResource ModalPrimaryButton}"
                                        FontSize="16"
                                        Visibility="Collapsed"
                                        Click="AddTechnicalCard_Click"/>
                            </Grid>

                            <Border x:Name="StatusBorder"
                                    Grid.Row="1"
                                    Margin="0,0,0,12"
                                    Padding="14,10"
                                    Background="#FFFFF1F1"
                                    BorderBrush="#FFD06A6A"
                                    BorderThickness="2"
                                    CornerRadius="8"
                                    Visibility="Collapsed">
                                <TextBlock x:Name="StatusText"
                                           FontSize="24"
                                           FontWeight="SemiBold"
                                           Foreground="#FF9F2F2F"
                                           TextWrapping="Wrap"/>
                            </Border>

                            <Grid Grid.Row="2">
                                <ScrollViewer x:Name="CardsScrollViewer"
                                              VerticalScrollBarVisibility="Auto"
                                              HorizontalScrollBarVisibility="Disabled">
                                    <ItemsControl x:Name="CardsList"
                                                  HorizontalContentAlignment="Stretch">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Margin="0,0,0,10"
                                                        Padding="0"
                                                        BorderThickness="0"
                                                        Background="Transparent"
                                                        HorizontalAlignment="Stretch"
                                                        HorizontalContentAlignment="Stretch"
                                                        Tag="{Binding}"
                                                        Click="TechnicalCard_Click"
                                                        Cursor="Hand">
                                                    <Border Style="{StaticResource ModalCardBorder}"
                                                            HorizontalAlignment="Stretch"
                                                            Background="{StaticResource ModalColorCardAltBrush}">
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding CardName}"
                                                                       FontSize="24"
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{StaticResource ModalColorTextBrush}"
                                                                       TextWrapping="Wrap"/>
                                                            <TextBlock Text="{Binding DescriptionPreview}"
                                                                       Margin="0,8,0,0"
                                                                       FontSize="17"
                                                                       Foreground="{StaticResource ModalColorTextBrush}"
                                                                       TextWrapping="Wrap"/>
                                                        </StackPanel>
                                                    </Border>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>

                                <TextBlock x:Name="EmptyText"
                                           Text="Технические карты не найдены"
                                           FontSize="24"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource ModalColorTextBrush}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed"/>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Viewbox>
    </Grid>
</UserControl>
