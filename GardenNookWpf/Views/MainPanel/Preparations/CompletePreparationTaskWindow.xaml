<Window x:Class="GardenNookWpf.Views.MainPanel.Preparations.CompletePreparationTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выполнить заготовку"
        Width="760"
        Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        SnapsToDevicePixels="True">
    <Border Style="{StaticResource ModalShellBorder}" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Style="{StaticResource ModalHeader}"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Отметить как выполненное"
                               Style="{StaticResource ModalHeaderTitle}"/>

                    <Button Grid.Column="1"
                            Content="×"
                            Style="{StaticResource ModalCloseButton}"
                            Click="HeaderClose_Click"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="16,14,16,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Полуфабрикат"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource ModalColorTextBrush}"/>

                <TextBlock x:Name="SemiFinishedNameText"
                           Grid.Row="1"
                           Margin="0,8,0,20"
                           FontSize="23"
                           FontWeight="Bold"
                           Foreground="{StaticResource ModalColorTextBrush}"
                           TextWrapping="Wrap"/>

                <TextBlock Grid.Row="2"
                           Text="Масса (г)"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource ModalColorTextBrush}"/>

                <TextBox x:Name="StockTextBox"
                         Grid.Row="3"
                         Margin="0,8,0,20"
                         Height="46"
                         FontSize="18"
                         VerticalContentAlignment="Center"/>

                <TextBlock Grid.Row="4"
                           Text="Дата изготовления"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource ModalColorTextBrush}"/>

                <DatePicker x:Name="ProductionDatePicker"
                            Grid.Row="5"
                            Margin="0,8,0,0"
                            Height="42"
                            FontSize="18"/>

                <TextBlock x:Name="ValidationText"
                           Grid.Row="6"
                           Margin="0,14,0,0"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#FFB33131"
                           Visibility="Collapsed"/>
            </Grid>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="16,0,16,16">
                <Button Content="Подтвердить"
                        Width="190"
                        Margin="0,0,10,0"
                        Style="{StaticResource ModalPrimaryButton}"
                        Click="Confirm_Click"/>
                <Button Content="Отмена"
                        Width="140"
                        Style="{StaticResource ModalSecondaryButton}"
                        Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
