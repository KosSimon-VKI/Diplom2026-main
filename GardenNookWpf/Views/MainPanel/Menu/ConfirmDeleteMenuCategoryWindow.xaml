<Window x:Class="GardenNookWpf.Views.MainPanel.Menu.ConfirmDeleteMenuCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="РЈРґР°Р»РµРЅРёРµ РєР°С‚РµРіРѕСЂРёРё РјРµРЅСЋ"
        Width="720"
        Height="390"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        SnapsToDevicePixels="True">
    <Border Style="{StaticResource ModalShellBorder}" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Style="{StaticResource ModalHeader}"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="РЈРґР°Р»РµРЅРёРµ РєР°С‚РµРіРѕСЂРёРё"
                               Style="{StaticResource ModalHeaderTitle}"/>

                    <Button Grid.Column="1"
                            Content="вњ•"
                            Style="{StaticResource ModalCloseButton}"
                            Click="HeaderClose_Click"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="18,16,18,12">
                <Border Style="{StaticResource ModalCardBorder}">
                    <StackPanel>
                        <TextBlock Text="РЈРґР°Р»РёС‚СЊ РєР°С‚РµРіРѕСЂРёСЋ РјРµРЅСЋ?"
                                   Style="{StaticResource ModalBodyText}"
                                   FontSize="25"
                                   FontWeight="SemiBold"
                                   TextWrapping="Wrap"/>
                        <TextBlock x:Name="CategoryNameText"
                                   Margin="0,10,0,0"
                                   Style="{StaticResource ModalBodyText}"
                                   FontSize="21"
                                   FontWeight="Bold"
                                   TextWrapping="Wrap"/>
                        <TextBlock x:Name="ItemsCountText"
                                   Margin="0,12,0,0"
                                   Style="{StaticResource ModalBodyText}"
                                   FontSize="18"
                                   TextWrapping="Wrap"/>
                        <TextBlock Text="Р•СЃР»Рё Рє РєР°С‚РµРіРѕСЂРёРё РїСЂРёРІСЏР·Р°РЅС‹ РїРѕР·РёС†РёРё РјРµРЅСЋ, СЃРµСЂРІРµСЂ РЅРµ РїРѕР·РІРѕР»РёС‚ СѓРґР°Р»РёС‚СЊ РµРµ."
                                   Margin="0,10,0,0"
                                   Style="{StaticResource ModalBodyText}"
                                   FontSize="17"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </Grid>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="16,0,16,16">
                <Button Content="РЈРґР°Р»РёС‚СЊ"
                        Width="180"
                        Margin="0,0,10,0"
                        Style="{StaticResource ModalPrimaryButton}"
                        Background="#FF742C27"
                        Click="Confirm_Click"/>
                <Button Content="РћС‚РјРµРЅР°"
                        Width="140"
                        Style="{StaticResource ModalSecondaryButton}"
                        Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
